<%=	form_for(current_user.relationships.build(inv_id:	@user.id),
						 	            html:	{	method:	:delete	})	do	|f|	%>
	 <div><%=	hidden_field_tag	:wish_id,	@user.id	%></div>
		<%=	f.submit	@label,	class:	"btn	btn-warning"	%>
<%	end	%>
